# Environment configuration for Docker deployment

# PostgreSQL
POSTGRES_PASSWORD=secure-password-change-me

# Flask/Discovery System
SECRET_KEY=your-secret-key-generate-with-secrets-token-hex-32
FLASK_ENV=production
FLASK_DEBUG=false

# AGI System
NODE_ENV=production

# LLM API Keys
OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key

# GitHub Integration
GIT_REPO_URL=owner/repository
GIT_ACCESS_TOKEN=ghp_your_github_token

# Internal service URLs (set by docker-compose, can override)
METRICS_API_URL=http://discovery:5000
DISCOVERY_URL=http://discovery:5000
DATABASE_URL=postgresql://agi_user:secure-password-change-me@postgres:5432/agi_system
